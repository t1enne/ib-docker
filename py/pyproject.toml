[project]
name = "ibkr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.3.2",
    "statsmodels>=0.14.0",
    "plotly-express>=0.4.1",
    "click>=8.3.1",
    "pyyaml>=6.0.3",
]
[tool.pyright]
venvPath = "."
venv = ".venv"

